import AdminClient from "@/components/AdminClient";

export const metadata = {
    title: "NEWTEX | Admin Panel",
    robots: "noindex, nofollow",
};

export default function AdminPage() {
    return <AdminClient />;
}